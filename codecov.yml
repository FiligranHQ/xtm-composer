# Codecov configuration for CI coverage reporting and patch-quality gating.
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        target: 80%
        threshold: 0%
        informational: false
